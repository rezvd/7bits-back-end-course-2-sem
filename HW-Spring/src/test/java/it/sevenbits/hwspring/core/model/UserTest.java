package it.sevenbits.hwspring.core.model;

import org.hamcrest.Condition;
import org.junit.Assert;
import org.junit.Test;

import java.util.ArrayList;
import java.util.List;
import java.util.UUID;

public class UserTest {

/*
    @Test
    public void creatingUserTest() {
        String id = UUID.randomUUID().toString();
        String username = "username";
        String password = "password";
        List<String> auths = new ArrayList<>();
        auths.add("USER");
        auths.add("ADMIN");
        User user = new User(id, username, password, auths);
        Assert.assertEquals(id, user.getId());
        Assert.assertEquals(username, user.getUsername());
        Assert.assertEquals(password, user.getPassword());
        Assert.assertEquals(auths, user.getAuthorities());
    }

    @Test
    public void creatingUserWithoutPasswordTest() {
        String id = UUID.randomUUID().toString();
        String username = "username";
        List<String> auths = new ArrayList<>();
        auths.add("USER");
        auths.add("ADMIN");
        User user = new User(id, username, auths);
        Assert.assertEquals(id, user.getId());
        Assert.assertEquals(username, user.getUsername());
        Assert.assertEquals(auths, user.getAuthorities());
    }

 */
}
